# meteor glowie edition
meteor with patches to remove the telemetry and capes

## how
0. run `cd meteor; git reset --hard HEAD; git pull origin master; cd -`
1. run build.sh
2. find built jar in meteor/build/libs
